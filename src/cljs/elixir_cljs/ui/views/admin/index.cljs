(ns elixir-cljs.ui.views.admin.index
  (:require
    [re-frame.core :refer [dispatch subscribe]])
  ;;   (:require-macros [reagent.ratom :refer [reaction]])
  )

(defn admin-view []
  (let []
    (fn []
      [:div
       [:h3 "Admin"]])))
